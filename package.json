{
  "scripts": {
    "serve": "parcel index.html",
    "start": "parcel ./*.html",
    "predeploy": "rm -rf dist && parcel build ./*.html --public-url https://ruslan-astratov.github.io/piggy-bank/",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "gh-pages": "^3.1.0",
    "moment": "^2.29.1"
  },
  "devDependencies": {
    "parcel": "*"
  }
}
